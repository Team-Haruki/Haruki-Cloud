# 解绑账号

## 用法
- `/解绑 [u序号|游戏 UID]`

## 参数
- `u1` `u2`：解绑自己的指定绑定序号。
- `游戏 UID`：解绑匹配的绑定账号。
- 空参数：不写参数时解绑当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 移除当前平台账号与游戏 UID 的绑定关系。

## 示例
- `/解绑`
- `/解绑 u2`
