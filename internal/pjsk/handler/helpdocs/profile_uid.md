# 查询 UID

## 用法
- `/uid [目标]`

## 参数
- `@用户`：查询被 @ 用户的绑定资料。
- `游戏 UID`：直接查询指定游戏账号。
- `u1` `u2` 等：查询自己的第几个绑定账号；不写时使用默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回目标绑定账号的游戏 UID；如果对方隐藏 UID，会按隐私设置脱敏。

## 示例
- `/uid`
- `/uid @123456`
