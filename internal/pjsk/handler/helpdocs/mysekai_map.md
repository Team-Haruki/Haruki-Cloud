# 烤森地图

## 用法
- `/烤森地图 [地图 ID...] [all] [force] [u序号]`

## 参数
- `地图 ID`：指定要查看的地图；可写多个。
- `all`：包含已采集资源。
- `force`：跳过时间检查。
- `u1` `u2` 等：选择自己的第几个绑定账号；不写时使用当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回 MySekai 地图资源分布图。

## 示例
- `/烤森地图`
- `/msm 3 force`
