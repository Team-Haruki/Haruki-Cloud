# HarukiBot 指令帮助

在任意指令后追加 `help`、`-h`、`--help` 或 `帮助` 可以查看对应模块帮助图。

## 通用参数
- 区服可用 `/jp` `/cn` `/tw` `/kr` `/en` 前缀指定，例如 `/jp查曲 miku`。
- 账号相关查询可用 `u1` `u2` 选择绑定列表中的账号，也可以在群聊中 `@用户` 查询对方。
- 不写区服时优先使用工具箱中的默认绑定账号。
