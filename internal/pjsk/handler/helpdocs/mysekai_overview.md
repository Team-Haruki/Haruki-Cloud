# 烤森总览

## 用法
- `/烤森总览 [地图 ID...] [all] [force] [u序号]`

## 参数
- `地图 ID`：只显示指定地图；可写多个。
- `all`：包含已采集资源。
- `force`：跳过时间检查。
- `u1` `u2` 等：选择自己的第几个绑定账号；不写时使用当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回 MySekai 资源地图总览。

## 示例
- `/烤森总览`
- `/msam 1 2 all`
