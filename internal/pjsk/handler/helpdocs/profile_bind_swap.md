# 交换绑定顺序

## 用法
- `/交换绑定 <u序号1> <u序号2>`

## 参数
- `u序号1`、`u序号2`：要交换的绑定位置，例如 `u1 u2`。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 交换两个绑定账号的顺序，影响之后 `u1` `u2` 的选择。

## 示例
- `/交换绑定 u1 u2`
