# 自定义资料卡

## 用法
- `/自定义资料卡 [u序号] [参数...]`

## 参数
- `u1` `u2`：选择绑定账号。
- 资料卡参数：按指令提示指定模板、模块、颜色或展示项。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 生成或更新自定义个人资料卡。

## 示例
- `/自定义资料卡`
