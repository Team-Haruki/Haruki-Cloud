// Code generated by ent, DO NOT EDIT.

package pjsk

import (
	"fmt"
	"haruki-cloud/database/pjsk/aliassubmissionban"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AliasSubmissionBan is the model entity for the AliasSubmissionBan schema.
type AliasSubmissionBan struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Platform holds the value of the "platform" field.
	Platform string `json:"platform,omitempty"`
	// PlatformUserID holds the value of the "platform_user_id" field.
	PlatformUserID string `json:"platform_user_id,omitempty"`
	// BannedBy holds the value of the "banned_by" field.
	BannedBy string `json:"banned_by,omitempty"`
	// BannedAt holds the value of the "banned_at" field.
	BannedAt     time.Time `json:"banned_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AliasSubmissionBan) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case aliassubmissionban.FieldID:
			values[i] = new(sql.NullInt64)
		case aliassubmissionban.FieldPlatform, aliassubmissionban.FieldPlatformUserID, aliassubmissionban.FieldBannedBy:
			values[i] = new(sql.NullString)
		case aliassubmissionban.FieldBannedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AliasSubmissionBan fields.
func (_m *AliasSubmissionBan) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case aliassubmissionban.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case aliassubmissionban.FieldPlatform:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field platform", values[i])
			} else if value.Valid {
				_m.Platform = value.String
			}
		case aliassubmissionban.FieldPlatformUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field platform_user_id", values[i])
			} else if value.Valid {
				_m.PlatformUserID = value.String
			}
		case aliassubmissionban.FieldBannedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field banned_by", values[i])
			} else if value.Valid {
				_m.BannedBy = value.String
			}
		case aliassubmissionban.FieldBannedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field banned_at", values[i])
			} else if value.Valid {
				_m.BannedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AliasSubmissionBan.
// This includes values selected through modifiers, order, etc.
func (_m *AliasSubmissionBan) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AliasSubmissionBan.
// Note that you need to call AliasSubmissionBan.Unwrap() before calling this method if this AliasSubmissionBan
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AliasSubmissionBan) Update() *AliasSubmissionBanUpdateOne {
	return NewAliasSubmissionBanClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AliasSubmissionBan entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AliasSubmissionBan) Unwrap() *AliasSubmissionBan {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("pjsk: AliasSubmissionBan is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AliasSubmissionBan) String() string {
	var builder strings.Builder
	builder.WriteString("AliasSubmissionBan(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("platform=")
	builder.WriteString(_m.Platform)
	builder.WriteString(", ")
	builder.WriteString("platform_user_id=")
	builder.WriteString(_m.PlatformUserID)
	builder.WriteString(", ")
	builder.WriteString("banned_by=")
	builder.WriteString(_m.BannedBy)
	builder.WriteString(", ")
	builder.WriteString("banned_at=")
	builder.WriteString(_m.BannedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AliasSubmissionBans is a parsable slice of AliasSubmissionBan.
type AliasSubmissionBans []*AliasSubmissionBan
