# 烤森大门升级

## 用法
- `/烤森大门升级 [大门 ID] [full] [all] [u序号]`

## 参数
- `大门 ID`：指定门/区域编号。
- `full`：显示完整升级材料。
- `all`：显示全部阶段。
- `u1` `u2` 等：选择自己的第几个绑定账号；不写时使用当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回 MySekai 大门升级需求和材料进度。

## 示例
- `/烤森大门升级`
- `/msg 2 full all`
