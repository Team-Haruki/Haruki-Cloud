# 烤森照片

## 用法
- `/msp <照片编号> [u序号]`

## 参数
- `照片编号`：必填，从 `1` 或 `-1` 开始；负数通常表示倒序。
- `u1` `u2` 等：选择自己的第几个绑定账号；不写时使用当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回指定 MySekai 照片。

## 示例
- `/msp 1`
- `/mysekai 照片 -1`
