// Code generated by ent, DO NOT EDIT.

package sekai

import (
	"fmt"
	"haruki-cloud/database/sekai/limitedtimemusic"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Limitedtimemusic is the model entity for the Limitedtimemusic schema.
type Limitedtimemusic struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// GameID holds the value of the "game_id" field.
	GameID int `json:"game_id,omitempty"`
	// MusicID holds the value of the "music_id" field.
	MusicID int `json:"music_id,omitempty"`
	// StartAt holds the value of the "start_at" field.
	StartAt int `json:"start_at,omitempty"`
	// EndAt holds the value of the "end_at" field.
	EndAt int `json:"end_at,omitempty"`
	// ServerRegion holds the value of the "server_region" field.
	ServerRegion string `json:"server_region,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Limitedtimemusic) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case limitedtimemusic.FieldID, limitedtimemusic.FieldGameID, limitedtimemusic.FieldMusicID, limitedtimemusic.FieldStartAt, limitedtimemusic.FieldEndAt:
			values[i] = new(sql.NullInt64)
		case limitedtimemusic.FieldServerRegion:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Limitedtimemusic fields.
func (_m *Limitedtimemusic) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case limitedtimemusic.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case limitedtimemusic.FieldGameID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field game_id", values[i])
			} else if value.Valid {
				_m.GameID = int(value.Int64)
			}
		case limitedtimemusic.FieldMusicID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field music_id", values[i])
			} else if value.Valid {
				_m.MusicID = int(value.Int64)
			}
		case limitedtimemusic.FieldStartAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field start_at", values[i])
			} else if value.Valid {
				_m.StartAt = int(value.Int64)
			}
		case limitedtimemusic.FieldEndAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field end_at", values[i])
			} else if value.Valid {
				_m.EndAt = int(value.Int64)
			}
		case limitedtimemusic.FieldServerRegion:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field server_region", values[i])
			} else if value.Valid {
				_m.ServerRegion = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Limitedtimemusic.
// This includes values selected through modifiers, order, etc.
func (_m *Limitedtimemusic) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Limitedtimemusic.
// Note that you need to call Limitedtimemusic.Unwrap() before calling this method if this Limitedtimemusic
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Limitedtimemusic) Update() *LimitedtimemusicUpdateOne {
	return NewLimitedtimemusicClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Limitedtimemusic entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Limitedtimemusic) Unwrap() *Limitedtimemusic {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("sekai: Limitedtimemusic is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Limitedtimemusic) String() string {
	var builder strings.Builder
	builder.WriteString("Limitedtimemusic(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("game_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.GameID))
	builder.WriteString(", ")
	builder.WriteString("music_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.MusicID))
	builder.WriteString(", ")
	builder.WriteString("start_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.StartAt))
	builder.WriteString(", ")
	builder.WriteString("end_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.EndAt))
	builder.WriteString(", ")
	builder.WriteString("server_region=")
	builder.WriteString(_m.ServerRegion)
	builder.WriteByte(')')
	return builder.String()
}

// Limitedtimemusics is a parsable slice of Limitedtimemusic.
type Limitedtimemusics []*Limitedtimemusic
