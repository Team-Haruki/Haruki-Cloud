// Code generated by ent, DO NOT EDIT.

package pjsk

import (
	"encoding/json"
	"fmt"
	"haruki-cloud/database/pjsk/gameaccount"
	"haruki-cloud/internal/pjsk/drawing"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GameAccount is the model entity for the GameAccount schema.
type GameAccount struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// PJSK game user ID
	UserID string `json:"user_id,omitempty"`
	// PJSK server region
	Server string `json:"server,omitempty"`
	// Profile card background settings stored as JSONB
	Bg *drawing.ProfileBgSettings `json:"bg,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GameAccountQuery when eager-loading is set.
	Edges        GameAccountEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GameAccountEdges holds the relations/edges for other nodes in the graph.
type GameAccountEdges struct {
	// Bindings holds the value of the bindings edge.
	Bindings []*UserBinding `json:"bindings,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BindingsOrErr returns the Bindings value or an error if the edge
// was not loaded in eager-loading.
func (e GameAccountEdges) BindingsOrErr() ([]*UserBinding, error) {
	if e.loadedTypes[0] {
		return e.Bindings, nil
	}
	return nil, &NotLoadedError{edge: "bindings"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GameAccount) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case gameaccount.FieldBg:
			values[i] = new([]byte)
		case gameaccount.FieldID:
			values[i] = new(sql.NullInt64)
		case gameaccount.FieldUserID, gameaccount.FieldServer:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GameAccount fields.
func (_m *GameAccount) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case gameaccount.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case gameaccount.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.String
			}
		case gameaccount.FieldServer:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field server", values[i])
			} else if value.Valid {
				_m.Server = value.String
			}
		case gameaccount.FieldBg:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field bg", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Bg); err != nil {
					return fmt.Errorf("unmarshal field bg: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GameAccount.
// This includes values selected through modifiers, order, etc.
func (_m *GameAccount) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBindings queries the "bindings" edge of the GameAccount entity.
func (_m *GameAccount) QueryBindings() *UserBindingQuery {
	return NewGameAccountClient(_m.config).QueryBindings(_m)
}

// Update returns a builder for updating this GameAccount.
// Note that you need to call GameAccount.Unwrap() before calling this method if this GameAccount
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GameAccount) Update() *GameAccountUpdateOne {
	return NewGameAccountClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GameAccount entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GameAccount) Unwrap() *GameAccount {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("pjsk: GameAccount is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GameAccount) String() string {
	var builder strings.Builder
	builder.WriteString("GameAccount(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(_m.UserID)
	builder.WriteString(", ")
	builder.WriteString("server=")
	builder.WriteString(_m.Server)
	builder.WriteString(", ")
	builder.WriteString("bg=")
	builder.WriteString(fmt.Sprintf("%v", _m.Bg))
	builder.WriteByte(')')
	return builder.String()
}

// GameAccounts is a parsable slice of GameAccount.
type GameAccounts []*GameAccount
