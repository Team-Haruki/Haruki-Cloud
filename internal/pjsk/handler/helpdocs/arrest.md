# 逮捕

## 用法
- `/逮捕 [目标]`

## 参数
- `@用户`：查询被 @ 用户的绑定资料。
- `游戏 UID`：直接查询指定游戏账号。
- `u1` `u2` 等：查询自己的第几个绑定账号；不写时使用默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回玩家等级、UID 显示状态、各难度 Clear/FC/AP 统计、挑战 Live 高分。

## 示例
- `/逮捕`
- `/逮捕 @123456`
- `/jp逮捕 123456789`
