# 打歌进度

## 用法
- `/打歌进度 [难度] [u序号]`

## 参数
- 难度：`easy` `normal` `hard` `expert` `master` `append` 或缩写。
- `u1` `u2` 等：选择自己的第几个绑定账号；不写时使用当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回各难度 Clear、FC、AP 等打歌进度。

## 示例
- `/打歌进度`
- `/歌曲进度 master u2`
