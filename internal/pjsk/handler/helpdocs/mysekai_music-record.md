# 烤森唱片

## 用法
- `/烤森唱片 [id] [关键词] [u序号]`

## 参数
- `id`：在结果中显示唱片 ID。
- `关键词`：歌曲名、唱片名或筛选文本。
- `u1` `u2` 等：选择自己的第几个绑定账号；不写时使用当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回 MySekai 唱片获取/持有情况。

## 示例
- `/烤森唱片`
- `/mss id miku`
