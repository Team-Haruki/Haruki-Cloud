# BPM 搜索

## 用法
- `/bpms <BPM数值> [难度]`
- `/bpm搜索 <BPM数值> [难度]`

## 参数
- `BPM数值`：必填，正数，可带小数。
- 难度：可选，限制指定难度谱面。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回包含该 BPM 的歌曲列表；即使只有一个匹配结果也按列表输出。

## 示例
- `/bpms 200`
- `/bpm搜索 200 expert`
