# 烤森资源

## 用法
- `/烤森资源 [all] [force] [u序号]`

## 参数
- `all`：包含已采集/已收获资源。
- `force`：跳过时间有效性检查，强制按当前快照渲染。
- `u1` `u2` 等：选择自己的第几个绑定账号；不写时使用当前区服默认绑定。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 返回 MySekai 资源点、采集状态和剩余时间。

## 示例
- `/烤森资源`
- `/msa all force`
