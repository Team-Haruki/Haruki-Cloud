# 绑定账号

## 用法
- `/绑定 <游戏 UID>`

## 参数
- `游戏 UID`：必填，PJSK 游戏内数字 UID。
- 区服前缀：可在指令前加 `jp` `cn` `en` `tw` `kr`，例如 `/jp查曲`；不写时使用默认区服。

## 输出
- 将当前平台账号与游戏 UID 绑定；同区服可绑定多个账号。

## 示例
- `/绑定 123456789`
- `/jp绑定 123456789`
